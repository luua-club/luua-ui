# Strictly enforce Node.js version requirements from package.json
engine-strict=true

# Automatically install peer dependencies when installing packages
auto-install-peers=true

# Hoist all dependencies to the root node_modules (improves compatibility)
shamefully-hoist=true

# Use hoisted node_modules structure (better for compatibility with some tools)
node-linker=hoisted
